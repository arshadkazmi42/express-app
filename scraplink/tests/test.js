const { expect } = require('chai');
  
// Write your test using below syntax
describe('description of test', () => {
  it('test name', () => {
    // Do something here
    expect(1).to.equal(1);
  });
});
